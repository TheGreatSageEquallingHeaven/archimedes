server:
  port: 6100
spring:
  application:
    name: archimedes
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TheGreatSageEquallingHeaven/archimedes.git
          search-paths: /src/main/resources/shared
          default-label: master
#eureka:
#  client:
#    service-url.defaultZone: http://localhost:3100/eureka/
